import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.noteflow.web',
  appName: 'NoteFlow',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
